[project]
name = "embed-lrc-flac"
version = "0.1.0"
description = "Embed LRC lyrics into FLAC files in batch"
requires-python = ">=3.10"
dependencies = ["mutagen>=1.47", "imageio-ffmpeg>=0.5.1"]

[tool.uv]
package = false